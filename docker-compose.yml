version: '3.7'

services:
  latex:
    build:
      context: .
      dockerfile: Dockerfile
    image: texlive:latest
    container_name: thesis
    environment:
      CLEAN: ${CLEAN}
    networks:
      - latex-net
    volumes:
      - ./src:/tex/src

networks:
  latex-net:
    name: latex-net
